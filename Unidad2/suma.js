/**
 * Función que suma dos valores
 * @param {*} x - Primer sumando
 * @param {*} y - Segundo sumando 
 * @returns Devuelve la suma de dos números
 */
function Suma(x, y){
    return x + y;
}